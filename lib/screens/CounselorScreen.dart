import 'package:flutter/material.dart';
class CounselorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('COUNSELOR'),
      
    );
  }
}